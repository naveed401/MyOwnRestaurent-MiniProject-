touch .gitignore
git config --global user.name your Name, (NAVEED)
git config --global user.your Email, naveeed.uet@gmail.com
git init
git add .
git commit -m "intial commit"
get status # Just to see
ssh-keygen -t ed25519 -C "your_email@example.com", naveeed.uet@gmail.com
tail <path to id_rsa.pub> # Add this key to uour GitHub account.
git remote add origin <ssh url.get>
git push origin master

for adding something new in existing project
get status
git add .
git commit -m "intial commit" i.e name of the folder
git push origin master


